name: Personal Assistant (Secretary & Architect)

on:
  schedule:
    - cron: '0 * * * *' # Hourly
  workflow_dispatch:
  push:
    branches: [ main ] # Keep for testing interactions

permissions:
  contents: write

jobs:
  assistant_routine:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup and Auth
        uses: ./.github/actions/setup-and-auth
        with:
          node-version: '20'
          google-credentials: ${{ secrets.GOOGLE_CREDENTIALS }}

      - name: 🧠 AI Architect Review
        env:
          GOOGLE_PROJECT_ID: ${{ secrets.GOOGLE_PROJECT_ID }}
          GOOGLE_CLOUD_LOCATION: us-central1
        run: |
          mkdir -p docs
          npx tsx scripts/daily_architect.ts

      - name: 🎩 Secretary Notification
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          npx tsx scripts/secretary.ts

      - name: Commit Reports & State
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore(secretary): update state, inbox, and reports [skip ci]"
          file_pattern: 'docs/architectural_suggestions.md scripts/data/*.json'
